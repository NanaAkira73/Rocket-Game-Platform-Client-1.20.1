query modpackRegions {
    modpackRegions(filter: {purchasable: null}) {
        regionId
        displayName
        purchasable
        iconAsset {
            assetKey
            downloadUrl
        }
    }
}
